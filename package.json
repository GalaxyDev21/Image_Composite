{
  "name": "image-layers",
  "version": "1.0.0",
  "description": "A node JS function (in javascript for running on a node server) for creating a composite image from several other image layers.",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "canvas": "^2.6.1"
  }
}
